/** Output by Antigravity IDE — shared visual rhythm for mesh + logo (seconds per full breathe). */
export const AMBIENT_CYCLE_SEC = 4.5 as const

/** GreenEnergyPulse + AI-leaf halo sync (AmbientEcosystem). */
export const ECO_PULSE_SEC = 9 as const
